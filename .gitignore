# ---- OS / editor ----
.DS_Store

# ---- frontend (Vite / Node) ----
node_modules/
dist/
.vite/
*.local

# ---- backend (Python) ----
__pycache__/
*.pyc
venv/
.venv/
backend/test.db

# ---- secrets ----
.env
.env.*
!.env.example
# Production frontend config holds only the public backend URL (no secrets) and
# must be committed so `vite build` in CI bakes VITE_API_BASE_URL into the bundle.
!frontend/.env.production

.mcp.json